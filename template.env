# Data used by scripts

# Resolve the root path of the repo
REPO_ROOT_PATH="$(git rev-parse --show-toplevel)"
#SCRIPT_DIR="$(dirname "$(realpath "${BASH_SOURCE[-1]}")")"

SCRIPT_PATH="${REPO_ROOT_PATH}/scripts"
SOURCE_PATH="${REPO_ROOT_PATH}/src"
SOURCE_TALOS_PATH="${SOURCE_DIR}/talos"
SOURCE_KUBERNETES_PATH="${SOURCE_DIR}/kubernetes"
CONFIG_PATH="${REPO_ROOT_PATH}/config"
CONFIG_PATCHES_PATH="${CONFIG_DIR}/patches"
CONFIG_SECRETS_PATH="${CONFIG_DIR}/secrets.yaml"

CLUSTER_NAME="<cluster-name>"

CONTROL_PLANE_IMAGE_URL="factory.talos.dev/installer/<hash>:<version>"

CONTROL_PLANE_IPS=(
<control-node-ip>
<control-node-ip>
<...>
)

WORKER_IMAGE_URL="factory.talos.dev/installer/<hash>:<version>"

WORKER_IPS=(
<worker-ip>
<worker-ip>
<...>
)

